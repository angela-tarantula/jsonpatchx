# Operation Schema

This module contains `OperationSchema`, the base class for built-in and custom
patch operations.

<!-- prettier-ignore -->
::: jsonpatchx.schema
    options:
      show_root_heading: false
      filters:
        - "!^_"

# Selectors

This module contains `JSONSelector`, the typed query selector used to target
multiple locations in a document.

<!-- prettier-ignore -->
::: jsonpatchx.selector
    options:
      show_root_heading: false
      show_root_toc_entry: false
      group_by_category: false
      filters:
        - "!^_"
        - "^__repr__$"

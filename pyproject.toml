[project]
name = "jsonpatch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jsonpointer>=3.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.1",
]

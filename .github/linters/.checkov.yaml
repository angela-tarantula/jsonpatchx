---
skip-check:
  # Demo and snapshot OpenAPI specs intentionally omit global auth requirements.
  - CKV_OPENAPI_4
  # Demo endpoints intentionally do not define per-operation auth requirements.
  - CKV_OPENAPI_5
  # JSON Patch request arrays are intentionally unbounded; maxItems is not enforced.
  - CKV_OPENAPI_21
skip-path:
  # This fixture intentionally includes non-standard JSON numeric literals (NaN/Infinity).
  - tests/compliance/rfc6902/jsonpatchx_nonfinite_tests.json
  # Vendored upstream compliance suites are test data, not this repo's workflow/config surface.
  - tests/compliance/rfc6902/external/
  - tests/compliance/rfc9535/external/

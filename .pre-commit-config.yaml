---
# Runs CI locally to save time on failures
minimum_pre_commit_version: "3.7.0"
repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: end-of-file-fixer
        priority: 10
      - id: trailing-whitespace
        priority: 20
      - id: check-merge-conflict
        priority: 0
      - id: check-json
        # This fixture intentionally includes non-standard JSON numeric literals (NaN/Infinity).
        # In super-linter, this is FILTER_REGEX_EXCLUDE.
        exclude: &json_nonfinite_exclude ^tests/compliance/rfc6902/jsonpatchx_nonfinite_tests\.json$
        priority: 0
      - id: check-yaml
        priority: 0

  - repo: https://github.com/rhysd/actionlint
    rev: v1.7.12
    hooks:
      - id: actionlint
        files: ^\.github/(workflows|actions)/.*\.ya?ml$
        priority: 0

  - repo: https://github.com/zizmorcore/zizmor-pre-commit
    rev: v1.23.1
    hooks:
      - id: zizmor
        files: ^\.github/(workflows|actions)/.*\.ya?ml$
        priority: 0

  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
        priority: 0

  - repo: https://github.com/igorshubovych/markdownlint-cli
    rev: v0.48.0
    hooks:
      - id: markdownlint
        name: markdownlint
        args:
          [
            --config,
            .github/linters/.markdownlint.yml,
            --ignore-path,
            .gitignore,
          ]
        priority: 0
      - id: markdownlint
        name: markdownlint --fix
        args:
          [
            --config,
            .github/linters/.markdownlint.yml,
            --ignore-path,
            .gitignore,
            --fix,
          ]
        priority: 30

  - repo: https://github.com/adrienverge/yamllint.git
    rev: v1.38.0
    hooks:
      # no way to respect gitignore
      - id: yamllint
        args: [-c, .github/linters/.yamllint.yml]
        priority: 0

  - repo: https://github.com/codespell-project/codespell
    rev: v2.4.2
    hooks:
      - id: codespell
        priority: 0

  - repo: local
    hooks:
      - id: ruff
        name: ruff
        entry: uv run ruff check
        language: system
        types: [python]
        priority: 0

      - id: ruff-format
        name: ruff format
        entry: uv run ruff format --exit-non-zero-on-format
        language: system
        types: [python]
        priority: 40

      - id: mypy
        name: mypy
        entry: uv run mypy --no-incremental .
        language: system
        pass_filenames: false
        priority: 0

      - id: update-openapi-snapshots
        name: update openapi snapshots
        entry:
          # In commit hooks, snapshot rewrites should fail so the next commit
          # attempt sees the regenerated files.
          uv run python scripts/update_openapi_snapshots.py
          --exit-non-zero-on-format
        language: system
        files: (^.*\.py$|^.*\.json$|^pyproject\.toml$|^uv\.lock$)
        pass_filenames: false
        priority: 50

      - id: checkov
        name:
          checkov
          # no way to respect gitignore
        entry:
          uv tool run --python 3.13 checkov --config-file
          .github/linters/.checkov.yaml --directory .
        language: system
        pass_filenames: false
        priority: 0

      - id: jscpd
        name: jscpd
        entry: npx --yes jscpd --gitignore --config .github/linters/.jscpd.json
        language: system
        types: [python]
        pass_filenames: false
        priority: 0

      - id: json-prettier
        name: json prettier --check
        entry:
          npx --yes prettier --check --ignore-path .gitignore --config
          .github/linters/.prettierrc.json
        language: system
        files: \.json$
        exclude: *json_nonfinite_exclude
        priority: 0

      - id: markdown-prettier
        name: markdown prettier --check
        entry:
          npx --yes prettier --check --ignore-path .gitignore --config
          .github/linters/.prettierrc.json
        language: system
        files: \.(md|markdown)$
        priority: 0

      - id: yaml-prettier
        name: yaml prettier --check
        entry:
          npx --yes prettier --check --ignore-path .gitignore --config
          .github/linters/.prettierrc.json
        language: system
        files: \.ya?ml$
        priority: 0

      - id: json-prettier
        name: json prettier
        entry:
          npx --yes prettier --write --ignore-path .gitignore --config
          .github/linters/.prettierrc.json
        language: system
        files: \.json$
        exclude: *json_nonfinite_exclude
        priority: 60

      - id: markdown-prettier
        name: markdown prettier
        entry:
          npx --yes prettier --write --ignore-path .gitignore --config
          .github/linters/.prettierrc.json
        language: system
        files: \.(md|markdown)$
        priority: 70

      - id: yaml-prettier
        name: yaml prettier
        entry:
          npx --yes prettier --write --ignore-path .gitignore --config
          .github/linters/.prettierrc.json
        language: system
        files: \.ya?ml$
        priority: 80

      - id: natural-language
        name: natural language (textlint terminology)
        entry:
          npx --yes --package textlint --package textlint-rule-terminology
          textlint --rule terminology
        language: system
        files: \.(md|markdown)$
        priority: 0

      - id: openapi
        name: openapi (spectral)
        entry:
          npx --yes @stoplight/spectral-cli lint --ruleset
          .github/linters/.openapirc.yml
        language: system
        files: \.json$
        exclude: *json_nonfinite_exclude
        priority: 0
  - repo: https://github.com/jumanjihouse/pre-commit-hooks
    rev: 3.0.0
    hooks:
      - id: shellcheck
        priority: 0
  # - repo: https://github.com/pylint-dev/pylint
  #   rev: v4.0.5
  #   hooks:
  #     - id: pylint
